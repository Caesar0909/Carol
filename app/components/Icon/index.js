// @flow
import React from 'react';
import SvgIcon from 'react-native-svg-icon';
import svgs from '../../assets/icons/svg/';

const Icon = (props: Object) => <SvgIcon {...props} svgs={svgs} />;

export default Icon;
