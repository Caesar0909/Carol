// @flow
import s from '../../helpers/spacing';

export default {
    'pill-list': {
        alignItems: 'flex-start',
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    'pill-list__item': {
        marginBottom: s('default'),
        marginRight: s('default')
    }
};
